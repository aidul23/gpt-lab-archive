# Copy to .env and fill in real values. Never commit .env.
SECRET_KEY=replace-with-a-long-random-string
ADMIN_PASSWORD=replace-with-a-strong-password
# Optional: use a hash instead of plaintext ADMIN_PASSWORD
# ADMIN_PASSWORD_HASH=
FLASK_DEBUG=0
FLASK_PORT=5001
